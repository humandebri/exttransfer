// app/page.tsx: Entrypoint that renders the EXT transfer shell for the full-page layout.
import TransferShell from "@/components/ext-transfer/transfer-shell";

export default function Home() {
  return <TransferShell />;
}
